<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <viewport content="width=device-width, initial-scale=1.0">
  <title>Update Post</title>
</head>
<body>
  <h1>Update Post</h1>
  <form action="/update-post/<%= postId %>", method="post">
  <label for="title">Title:</label>
  <input type="text" id="title" name="title" value="<%= posts[postId].title %>" required>
  <br>
  <label for "img_url">Image URL:</label>
  <input type="url" id="img_url" name="img_url" value="<%= posts[postId].img_url %>" required>
  <br>
  <label for="content">Content:</label>
  <textarea id="content" name="content" rows="4" required><%= posts[postId].content %></textarea>
  <br>
  <button type="submit">Update</button>
</body>
</html>